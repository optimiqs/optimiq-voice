# Development TLS material, generated by ./generate-dev-certs.sh.
#
# Nothing here is ever committed: the keys have no passphrase and the CA is trusted only by this
# checkout, so a copy in version control would be a private key in version control that also
# happens to be useless to anybody else. Run the script to (re)create them.
*.pem
*.csr
*.srl
