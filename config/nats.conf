# NATS server configuration for the Optimiq Voice platform.
#
# Mounted read-only at /etc/nats/nats.conf by both compose files; the `nats` service runs
# `-c /etc/nats/nats.conf`. A config file and a command line are NOT merged by nats-server, so the
# `-js --store_dir /data` flags this service used to carry had to move into the `jetstream` block
# below rather than sit alongside the file.
#
# WHY THIS FILE EXISTS
# --------------------
# Without it the broker accepts every anonymous connection on 4222. That is not a theoretical
# exposure on this platform: `rpc.sip.v1.credential` is a request/reply subject that hands out SIP
# password derivations to apps/sipd, the CDR and call-event streams carry every tenant's call
# metadata, and the `routing-cache` KV bucket is the dial plan. Anything that could reach the port
# could read all of it and publish forged events onto it, across every tenant at once.
#
# CREDENTIALS COME FROM THE ENVIRONMENT
# ------------------------------------
# `$NATS_USER` and friends are resolved by nats-server: a `$NAME` reference that is not defined in
# this file is looked up in the process environment. Compose passes them through from `.env`, so no
# password is ever written here or in compose.yaml.
#
# An UNRESOLVED reference is a hard startup error ("variable reference for 'NATS_USER' can not be
# resolved"), not a silent fallback — a stack brought up without these variables fails to start the
# broker instead of quietly starting an open one. That now covers TEN names rather than four: the
# four per-service credentials below are as required as the shared one.
#
# TLS
# ---
# There is deliberately no `tls` block HERE. Transport security is an OVERLAY: `config/nats-tls.conf`
# includes this file and adds the block, and `compose.tls.yaml` points the container at it and mounts
# the certificates. See the header of `config/nats-tls.conf` for why the split is that way round.

# The client port. `expose`d on the compose network; published to the host by compose.dev.yaml only.
port: 4222

# JetStream. Load-bearing rather than an optimisation: without it the engine cannot apply its
# stream/KV definitions at boot, the `channels` and `routing-cache` buckets do not exist, and every
# `cdr.leg.write` is refused.
jetstream {
  store_dir: "/data"
}

# TWO ACCOUNTS, because they are two different jobs.
#
# An account is a subject-space boundary: accounts cannot see each other's subjects, and JetStream
# assets belong to exactly one of them. Defining any account also REMOVES the implicit `$G` global
# account that unauthenticated clients would otherwise land in, which is what turns an anonymous
# connect into an "Authorization Violation" rather than a working session.
#
#   OPTIMIQ  every platform service — apps/api, apps/engine, apps/mediad, apps/sipd and the
#            operational scripts. ONE account, because they share a subject space by design: the
#            engine consumes what the API publishes, the CDR writer consumes what the engine
#            publishes, and JetStream assets belong to exactly one account so splitting the
#            services across accounts would mean the streams could only exist in one of them.
#
#   $SYS     the server's own account: `$SYS.>` monitoring, `nats server report`, account and
#            JetStream introspection. Kept apart from OPTIMIQ so an operator credential is not the
#            same secret every service container holds, and so a leaked service password cannot
#            read server internals. `$SYS` is nats-server's default system-account name, so naming
#            the account is all it takes — no `system_account` directive.
#
# FIVE USERS IN ONE ACCOUNT
# -------------------------
# One account, but no longer one identity. Each service authenticates as itself and carries a
# permission set built from what that service's code actually publishes and subscribes to. The
# permissions are ALLOW-LISTS: anything not named is refused, so a subject added to a service
# without being added here fails loudly at that service rather than quietly succeeding everywhere.
#
# What this buys, concretely:
#
#   * apps/mediad cannot read `rpc.sip.v1.credential`. A compromised media plane — the process with
#     the widest network exposure on the platform, since it terminates RTP from the internet —
#     cannot ask the control plane for a tenant's SIP password derivations.
#   * apps/sipd cannot write a CDR leg or a call event, and that stayed true when it became the
#     signalling plane. It now publishes dialog lifecycle, answers a command surface and holds two
#     read models it did not have before — and it still holds NO grant on `cdr.leg.v1.*`, NO grant
#     on `calls.evt.v1.>`, and NO grant on any `rpc.media.v1.` subject. Each absence is a different
#     boundary and each is deliberate.
#
#     `cdr.leg.v1.*` is the billing ledger. The edge is the process an unauthenticated stranger can
#     send a packet to, so it is the last one that should be able to write a row somebody is
#     invoiced for. It publishes `sip.evt.v1.…dialog.terminated` instead — the FACT that a dialog
#     ended, with the Q.850 cause it actually read off the wire — and the engine decides whether
#     that becomes a leg.
#
#     `calls.evt.v1.>` is the business account of a call, and the two vocabularies must not both be
#     authoritative. A `dialog.answered` is one leg's SIP transaction completing; a
#     `channel.answered` is a customer being connected to somebody. The engine is what turns the
#     first into the second, and an edge that could publish the second directly could announce a
#     call that never reached a routing decision.
#
#     `rpc.media.v1.` is the media plane. The SIP edge never talks to apps/mediad — not to allocate
#     a session, not to bridge one, not to tap one. `mediad` writes every SDP offer and answer, and
#     the edge carries them as opaque bodies. Two processes that never address each other cannot
#     develop a coupling nobody documented, and it is what keeps "who may listen to a call" a
#     question with exactly one answerable subject on this broker.
#   * Neither Go service can read the other's KV bucket, or the dial plan.
#   * apps/engine cannot write `routing-cache`, `did-index`, `queue-membership`, `trunks` or
#     `sip-acl` — the five buckets the CONTROL plane owns. It reads and watches the first three; the
#     last two it does not touch at all, since they are the SIP edge's read models. A bug in the
#     call path cannot rewrite a tenant's routing, and cannot widen the ACL that admits a carrier.
#   * apps/api cannot write `channels`, `park-claims` or `conference-claims` — the three buckets
#     the CALL path owns.
#
# WHY api AND engine GET `$JS.API.>` AND THE GO SERVICES DO NOT
# -------------------------------------------------------------
# JetStream is not a subject family, it is an RPC surface reached over `$JS.API.>` request subjects
# with replies on `_INBOX.>`. Denying it does not degrade JetStream, it breaks it — usually as a
# timeout rather than as an authorization error, because the client is waiting for a reply the
# server refused to let it publish for.
#
# The precise subjects depend on asset names AND, for consumers, on the consumer name and the
# filter subject, which appear as further tokens:
#
#   $JS.API.STREAM.CREATE.<stream>
#   $JS.API.CONSUMER.CREATE.<stream>.<consumer>.<filter subject, dots and all>
#   $JS.API.DIRECT.GET.<stream>.<subject>
#
# For apps/api and apps/engine that surface is genuinely large and genuinely moving: between them
# they create eight streams and ten KV buckets, run four named durable consumers
# (`cdr-leg-writer`, `cdr-recording-writer`, `pbx-emergency-notifier`, `pbx-voicemail-writer`) and
# open ANONYMOUS ordered consumers for every KV `watch()` and `keys()` — whose names are generated
# per call and whose filter subjects become subject tokens. An allow-list enumerating those would
# be exact today and wrong the first time somebody renames a consumer or adds a `watch`, and the
# failure would be a call path that stops delivering rather than a config error. So the two
# TypeScript services get the JetStream API PREFIX for this account and the tightening stops there,
# deliberately and on the record. They still cannot reach another account's assets: `$JS.API.>` is
# account-scoped, so it is the JetStream surface of OPTIMIQ and nothing else.
#
# apps/mediad and apps/sipd are the opposite case and are scoped precisely, because their JetStream
# use is small, fixed and enumerable: one KV bucket for mediad, and five for sipd — `registrations`
# and `sip-dialogs` written, `presence`, `trunks` and `sip-acl` read — with no streams created and no
# durable consumers. Their entries below are the exact list, and if one of them ever grows a consumer
# it will fail at boot with an authorization error naming the subject — which is the right way to
# find out.
#
# The read/write split inside that list is the interesting half, and `sip-acl` is why. The edge reads
# the networks it may accept unauthenticated INVITEs from and CANNOT write them; apps/api writes them
# and never reads them back. That is not symmetry for its own sake — it is what stops the process
# with a socket open to the internet from widening the boundary that admits it.
#
# `_INBOX.>` IS GRANTED TO EVERY USER, BOTH WAYS
# ----------------------------------------------
# Request-reply needs it on both sides and it cannot be narrowed usefully: a requester subscribes to
# the inbox IT generated (`_INBOX.<nuid>.<nuid>`, chosen by the client at connect time), and a
# responder must PUBLISH to an inbox somebody else generated. Every JetStream API call and every
# acked publish is also a request-reply. So `_INBOX.>` appears on both lists for all five users, and
# the isolation comes from the business subjects instead.
accounts {
  OPTIMIQ: {
    jetstream: enabled
    users: [
      # ---------------------------------------------------------------------------------------
      # dev / admin — the shared identity, and the ONLY one with no permission block.
      #
      # It is what the root `.env` still sets as NATS_USER/NATS_PASS, what the `nats` CLI connects
      # with, what every `pnpm verify:*` harness in apps/api/scripts uses, and what a deployment
      # that has not yet split its credentials falls back to (see
      # `packages/config/src/nats-credentials.ts`). The verify scripts publish and subscribe across
      # the whole taxonomy on purpose — they stand in for whichever service they are exercising —
      # so scoping this user would mean maintaining a sixth permission set that is the union of the
      # other four.
      #
      # It is an OPERATOR credential. Nothing in compose passes it to a service container any more.
      { user: $NATS_USER, password: $NATS_PASS }

      # ---------------------------------------------------------------------------------------
      # apps/api — the control plane.
      #
      # Publishes three event families, answers seven RPCs, owns SIX KV buckets and reads two
      # more, and runs the four durable CDR/PBX consumers. The seventh RPC is `rpc.authz.v1.check`,
      # which is the first one that is not about telephony at all — see its entry below.
      #
      # Six rather than four since the SIP edge got read models: `trunks` and `sip-acl` joined
      # `routing-cache`, `did-index`, `queue-membership` and `agent-state`. Both are written here
      # and nowhere else, and the write list below is the whole of that claim.
      {
        user: $NATS_API_USER
        password: $NATS_API_PASS
        permissions: {
          publish: {
            allow: [
              # Events it emits. `agent.state` on the `_all` queue scope, `mwi.updated` per
              # mailbox, and the device-provisioning trail.
              "queue.evt.v1.>"
              "voicemail.evt.v1.>"
              "provision.evt.v1.*"
              # The KV buckets the CONTROL plane owns and writes.
              "$KV.routing-cache.>"
              "$KV.did-index.>"
              "$KV.queue-membership.>"
              "$KV.agent-state.>"
              # The SIP edge's two derived read models, written here and read by apps/sipd. Same
              # seam `did-index` occupies, one process further out: apps/sipd is Go, holds no
              # pbx-db handle and must not grow one, because a database on the INVITE path is the
              # thing this architecture spends its budget avoiding — and the INVITE path is the one
              # code path whose rate an attacker chooses.
              #
              # `trunks` carries a trunk's dialable configuration: proxy, auth user, transport,
              # register expiry, and `sipSecretRef` — which is a HANDLE into the secret manager,
              # exactly as it is in the column. The password itself never lands on this broker, and
              # that is a property of what the projection writes rather than of this grant.
              #
              # `sip-acl` carries the source networks the edge accepts unauthenticated traffic
              # from. It is the one bucket here whose entries are a SECURITY boundary, so note what
              # this line is: a write grant held by the control plane ALONE. apps/sipd's grants
              # below are read-only on both buckets, which is what stops a compromised SIP edge
              # from widening the ACL that admits it.
              "$KV.trunks.>"
              "$KV.sip-acl.>"
              # `presence` — and this is the ONE bucket apps/api shares a write with apps/engine, so
              # it is worth saying exactly what each of them writes and why that is not a conflict.
              #
              # The engine writes per-EXTENSION device state, keyed by extension number and filtered
              # through the tenant's routing artifact so only real extensions produce a key. The API
              # writes per-CALL-FLOW switch state, keyed by the flow's toggle code (`*281`) or its
              # dialable number. The two key spaces cannot overlap: a toggle code that IS an
              # extension number is a compile-time error, which is what makes the separation a
              # property rather than a convention.
              #
              # Both are read by apps/sipd, which serves a BLF subscription from this bucket and does
              # not care which writer produced the value — a phone watching `*281` is watching a
              # presence key exactly like a phone watching `1001`. That is the whole reason the
              # call-flow lamp needed no new bucket, no new event package and no change to sipd.
              "$KV.presence.>"
              # Click-to-call: the control plane asking the engine to place a call. The ONLY subject
              # apps/api publishes onto the engine's own RPC surface. Publish and not subscribe —
              # the reply comes back on `_INBOX.>`, which is already granted below.
              "rpc.engine.v1.originate"
              # The session protocol's verb channel: the control plane relaying one command from an
              # external application onto the leg it was given control of. The trailing token is the
              # OWNING engine instance's id, so this wildcard is one token wide and covers every
              # replica — the same shape, and the same reason, as the engine's own
              # `rpc.engine.v1.park-handoff.*`.
              #
              # Publish and NOT subscribe: the reply comes back on `_INBOX.>`. Note there is no
              # matching grant for the ANNOUNCE that starts a session — that one points the other
              # way and is on the subscribe list below.
              "rpc.engine.v1.session-verb.*"
              # In-conference moderation: mute, deafen, kick, re-level one member of a live room, or
              # lock the room. Same one-token wildcard as the verb channel above it, and for the same
              # reason — the trailing token is an OWNING engine instance's id — but a DIFFERENT
              # power, which is why it is a separate line rather than a wider wildcard over
              # `rpc.engine.v1.*`.
              #
              # The verb channel commands a leg an application was HANDED. This one commands a leg
              # that was handed to nobody: any participant of any room in the tenant, on the strength
              # of an operator's `conferences.moderate` grant. Those are worth being able to withdraw
              # independently, and a family wildcard would make that impossible without also taking
              # away click-to-call.
              #
              # Publish and NOT subscribe: the reply comes back on `_INBOX.>`.
              "rpc.engine.v1.conference-control.*"
              # JetStream: stream and bucket creation, durable consumer creation, message acks.
              # See the note above on why this is the prefix and not an enumeration.
              "$JS.API.>"
              "$JS.ACK.>"
              # RPC replies, and the reply side of every JetStream request.
              "_INBOX.>"
            ]
          }
          subscribe: {
            allow: [
              # The live WebSocket fan-out's core subscriptions (`live-hub.service.ts`). The
              # fourth family, `trunk.evt.v1.>`, joined with the trunks live topic: the durable
              # status WRITER does not need it — a JetStream consumer's deliveries ride
              # `_INBOX.>` and its pulls are `$JS.API.>` requests, both granted below — but the
              # live hub's per-org subscription is a CORE subscribe, and the subject is the
              # permission.
              "calls.evt.v1.>"
              "queue.evt.v1.>"
              "voicemail.evt.v1.>"
              "trunk.evt.v1.>"
              # The seven subjects it RESPONDS on. `rpc.sip.v1.credential` is the raw-wire one
              # behind the `optimiq-api-sip-credentials` queue group; the rest are the Nest
              # microservice's `@MessagePattern`s.
              "rpc.routing.v1.resolve"
              "rpc.voicemail.v1.list"
              "rpc.sip.v1.credential"
              # The `pbx` family: the self-service star codes a handset dials. Two writes and one
              # read, enumerated rather than granted as `rpc.pbx.v1.*` for the reason the engine's
              # media list states — this list is the record of what each service actually answers,
              # so a subject added to the code without being added here fails at that service with
              # its own name in the log instead of quietly working everywhere.
              #
              # `extension-feature` and `last-caller` were missed when their responders landed;
              # they are here now, beside the one that made the gap visible. A deployment that has
              # been running without them has had `*72` and `*69` announcing "not available",
              # which is the honest failure the walker was built to produce and is still a failure.
              "rpc.pbx.v1.extension-feature"
              "rpc.pbx.v1.last-caller"
              "rpc.pbx.v1.file-greeting"
              # The FIRST non-pbx, non-routing subject this service answers: "may this subject do
              # these things in that organization?". The contract has existed since the first wave
              # of packages/events and had no responder anywhere until `*0` supervision needed one —
              # the engine cannot tap a live call without asking, and there is no session on a desk
              # phone for it to ask with.
              #
              # Granted ALONE and not as `rpc.authz.v1.*`, for exactly the reason the pbx family
              # above is enumerated: this list is the record of what apps/api actually answers, so a
              # subject added to the code without being added here fails at this service with its own
              # name in the log instead of quietly working everywhere. A family wildcard would also
              # pre-authorise whatever the next `rpc.authz.v1.` subject turns out to be, and the next
              # one is as likely to be a WRITE — issuing a grant rather than reading one — as it is
              # to be another read.
              "rpc.authz.v1.check"
              # The session protocol's front door: the engine offering a call that reached an
              # `application` destination to whoever is holding that application's socket.
              #
              # TWO wildcard tokens, and the only entry on any list in this file with more than one.
              # They are `<orgId>.<applicationToken>` and they are not a family wildcard in
              # disguise: THE SUBJECT IS THE REGISTRATION. A replica subscribes to exactly the
              # applications whose sockets it holds and unsubscribes when the last one closes, so an
              # application nobody has claimed produces `no responders available` immediately and the
              # engine announces to the caller instead of waiting out a timeout. Enumerating the
              # names is therefore impossible by construction — they are tenant data, created and
              # destroyed by a WebSocket connecting, not configuration.
              #
              # What the grant still bounds is the shape: two tokens and no more, so nothing can
              # subscribe to `rpc.session.v1.>` and see every tenant's announcements at once.
              "rpc.session.v1.announce.*.*"
              # NOTHING is added here for the webhook dispatcher, and that is worth stating rather
              # than leaving to be rediscovered: its four durable consumers read the CALLS, QUEUES,
              # VOICEMAIL and CDR streams, but a JetStream consumer does not SUBSCRIBE to the
              # stream's subjects — the server delivers to an inbox and the pulls are `$JS.API.>`
              # requests. Both are already granted. A subject list here would look like the
              # permission and would in fact be dead configuration.
              # The two buckets it READS and watches but never writes — sipd owns `registrations`,
              # the engine owns `channels`.
              "$KV.registrations.>"
              "$KV.channels.>"
              "$KV.agent-state.>"
              # The waiting line, READ ONLY. It is written by every engine instance holding a caller
              # and by nothing else; apps/api snapshots and watches it so a wallboard gets
              # waiting-count, positions and longest-wait without replaying events it may have
              # missed. Deliberately absent from the publish list above: a control plane that could
              # write this could evict a caller from a queue by editing a form.
              "$KV.queue-waiting.>"
              # The conference claims, READ ONLY, and this one grant is what makes in-conference
              # moderation addressable at all.
              #
              # The value names the room's bridge and, per engine instance, how many members that
              # instance holds and whether one of them is a moderator. The api reads it for two
              # things: the live conferences topic snapshots the rooms out of it (which rooms are
              # up, how many people are in each, whether a moderator has arrived), and the
              # moderation endpoints read the CONTRIBUTOR list to know which engine instances to
              # address `rpc.engine.v1.conference-control.<instance>` at.
              #
              # Deliberately absent from the publish list above, unlike `presence` which apps/api
              # shares a write on: a claim is an OWNERSHIP record between engine instances, and a
              # control plane that could write one could hand a live room's bridge to an instance
              # that is not in it — which is the split-brain the claim exists to prevent, caused by
              # the one process that is not on the call path.
              "$KV.conference-claims.>"
              # Durable consumer deliveries, KV watch deliveries, every JetStream API reply.
              "_INBOX.>"
            ]
          }
        }
      }

      # ---------------------------------------------------------------------------------------
      # apps/engine — the call path.
      #
      # The busiest identity: it emits the whole call and queue vocabulary, writes CDR legs and
      # voicemail deposits into JetStream, calls six RPCs — one of which it also ANSWERS, to hand a
      # parked call to another engine instance — and applies the stream/KV definitions at boot
      # (`ENGINE_ENSURE_STREAMS`, default on).
      {
        user: $NATS_ENGINE_USER
        password: $NATS_ENGINE_PASS
        permissions: {
          publish: {
            allow: [
              # The call vocabulary — channel lifecycle, conference, park, transfer, pickup and
              # the Kari's Law `call.emergency.dialed` seam.
              "calls.evt.v1.>"
              # Queue caller/agent facts.
              "queue.evt.v1.>"
              # `message.left` only, but the subject shape is per-mailbox so the filter is the
              # family. `mwi.updated` on the same family belongs to apps/api; the split is by
              # event name inside a shared family and NATS permissions cannot express that
              # without enumerating orgs, so the family is granted and the contract holds it.
              "voicemail.evt.v1.>"
              # Trunk status write-back: the engine translating the media server's qualify
              # transitions into `status.changed` facts about a tenant's `trunk` row. The family
              # and not a narrower filter, deliberately: the one event name is DOTTED, so the
              # concrete subject is SEVEN tokens (`trunk.evt.v1.<org>.<trunk>.status.changed`)
              # and a filter written as `trunk.evt.v1.*.*.*` would match none of them — the same
              # token arithmetic the mediad entry below records for `media.evt.v1`. A `>` tail
              # cannot mis-count.
              "trunk.evt.v1.>"
              # The billing ledger. Acked JetStream publish with a `Nats-Msg-Id`.
              "cdr.leg.v1.*"
              # The RPCs it calls.
              "rpc.routing.v1.resolve"
              "rpc.voicemail.v1.list"
              # The self-service star codes, answered by apps/api. Two of them WRITE — a handset
              # changing its own forwarding, and a handset filing the greeting it just recorded —
              # which is why they are named one at a time rather than granted as `rpc.pbx.v1.*`:
              # the ability to replace what a mailbox says is worth being able to withdraw without
              # also withdrawing `*69`.
              "rpc.pbx.v1.extension-feature"
              "rpc.pbx.v1.last-caller"
              "rpc.pbx.v1.file-greeting"
              # The authorization question, asked of apps/api. Its caller is the `*0` supervision
              # runtime: before ONE byte of audio is tapped off somebody's live conversation, the
              # engine asks whether the extension that dialled the code holds `calls.supervise` in
              # that tenant. The engine has no database and a handset carries no session, so the
              # extension NUMBER is the whole of the identity it can offer — and the responder
              # resolves that number to a row, to its primary user and to that user's role before
              # answering, because a number on a shared broker is a claim.
              #
              # AN ENGINE THAT CANNOT REACH THIS MUST REFUSE. Not "fall back to allowing", not "allow
              # and log": there is no safe default on this subject, because the failure mode of
              # guessing wrong is that a stranger listens to a customer call and neither party is
              # ever told. Fail-open here would also make the broker's own availability an
              # authorization boundary — anybody who can make this request time out could grant
              # themselves supervision — which is the precise inversion an allow-list exists to
              # prevent. So a timeout, a permissions violation on this line, and an explicit
              # `allowed: false` must all end the same way at the engine: no tap, and the caller
              # hears the unavailable announcement.
              "rpc.authz.v1.check"
              "rpc.media.v1.allocate-session"
              "rpc.media.v1.bridge-sessions"
              "rpc.media.v1.unbridge-sessions"
              "rpc.media.v1.release-session"
              # Rung 1's playback pair. Enumerated rather than granted as `rpc.media.v1.*`, for the
              # same reason the four above it are: this list is the record of what the engine
              # actually calls, so a subject added to the code without being added here fails at
              # that service with the subject named instead of quietly succeeding everywhere.
              "rpc.media.v1.start-playback"
              "rpc.media.v1.stop-playback"
              # Rung 3's digit generator and rung 4's recording pair. Same enumeration, same reason.
              "rpc.media.v1.send-dtmf"
              "rpc.media.v1.start-recording"
              "rpc.media.v1.stop-recording"
              # Supervision's media half: attach a one-way copy of a live session's audio to a
              # supervisor's session, and detach it again. Same enumeration and the same reason as
              # every `rpc.media.v1.` line above it, plus one that is specific to this pair — a tap
              # is the only media command on this platform that gives one party's audio to a THIRD
              # party, so it is the one whose grant an operator is most likely to want to withdraw
              # on its own. A `rpc.media.v1.*` wildcard would make that impossible without also
              # withdrawing playback, recording and bridging, which is to say without taking the
              # phone system down.
              #
              # `untap-session` is granted beside `tap-session` and never separately: an engine that
              # could start a tap and not stop one would leave a supervisor attached to a
              # conversation after they hung up, which is worse than never having tapped at all.
              "rpc.media.v1.tap-session"
              "rpc.media.v1.untap-session"
              # Rung 5's two state commands: take one leg out of the conversation (with music), and
              # gate one direction of one leg. Same enumeration and the same reason as every
              # `rpc.media.v1.` line above them.
              #
              # Two lines rather than one, because they are two different powers over a live call and
              # an operator may want to withdraw one. `mute-session` stops a party being heard and is
              # invisible to them — it is what a conference moderator's mute button is — while
              # `hold-session` takes them out of the conversation in both directions and plays them
              # music, which is what an agent's hold key is. Granting the second is not thereby
              # granting the first.
              "rpc.media.v1.mute-session"
              "rpc.media.v1.hold-session"
              # The SIP edge's dialog command surface: the engine driving apps/sipd as its signalling
              # edge, exactly as the `rpc.media.v1.` lines above drive apps/mediad as its media edge.
              #
              # The trailing token is the OWNING sipd instance's id, so each wildcard is one token
              # wide and covers every replica — the same shape, and the same reason, as
              # `rpc.engine.v1.park-handoff.*` below. Instance-addressed rather than queue-grouped
              # because a SIP dialog's transaction state, its retransmission timers and its socket
              # are all local to one process, and its `Contact` header has already told the far end
              # which host to send mid-dialog requests to (RFC 3261 §12.1.1). A second sipd cannot
              # answer this call, cannot retransmit its 2xx and cannot BYE it.
              #
              # Enumerated rather than granted as `rpc.sip.v1.*`, for the reason every
              # `rpc.media.v1.` line above is — this list is the record of what the engine actually
              # calls — plus one specific to this family: `rpc.sip.v1.credential` and
              # `rpc.sip.v1.transfer` point the OTHER way and are apps/api's and apps/sipd's to
              # answer. A family wildcard here would silently hand the engine the power to answer
              # for the credential responder, which is the subject that hands out SIP passwords.
              "rpc.sip.v1.ring.*"
              "rpc.sip.v1.answer.*"
              "rpc.sip.v1.hangup.*"
              "rpc.sip.v1.reinvite.*"
              # Outbound. FLAT rather than instance-addressed, and the only sipd command that is:
              # the four above act on a dialog that already exists on exactly one process, while
              # this one CREATES the dialog and so has no owner to find. Any sipd may place the
              # call; the reply carries the instance that took it, and every subsequent command on
              # that leg is addressed there. The same distinction, and the same queue-group answer,
              # as `rpc.engine.v1.originate` versus `rpc.engine.v1.park-handoff.*`.
              "rpc.sip.v1.originate"
              # The one RPC it both CALLS and ANSWERS: engine to engine, to collect a call parked
              # on another instance. The trailing token is the OWNING instance's id, so this
              # wildcard is one token wide and covers every replica — an enumeration would need a
              # broker restart every time a pod is rescheduled. Publish and subscribe are both
              # granted because every instance is a requester and a responder at once, and which
              # one it is on a given call depends on where the phone that dialled the orbit landed.
              "rpc.engine.v1.park-handoff.*"
              # The session protocol's front door, pointing at apps/api. Publish only — the reply
              # rides `_INBOX.>` — and the two-token wildcard is `<orgId>.<applicationToken>`; see
              # the api's subscribe list for why a registration cannot be enumerated here.
              "rpc.session.v1.announce.*.*"
              # The KV buckets the CALL path owns. `routing-cache`, `did-index` and
              # `queue-membership` are deliberately absent: the engine reads and watches those,
              # apps/api writes them.
              "$KV.channels.>"
              "$KV.agent-state.>"
              "$KV.park-claims.>"
              "$KV.conference-claims.>"
              # The waiting line. Written by EVERY instance holding a caller for a queue, under
              # compare-and-set — it is jointly held rather than claimed, which is why it sits here
              # and not with the two claim buckets it is listed beside.
              "$KV.queue-waiting.>"
              # `presence` — per-extension device state, the value a BLF key renders. The engine is
              # its ONLY writer, because it is the only process that sees a channel change call
              # state. It appears on the subscribe list too: the value is a MERGE POINT across engine
              # instances (see extensionPresenceSchema), so a write is a compare-and-swap that has to
              # read the current revision first.
              "$KV.presence.>"
              "$JS.API.>"
              "$JS.ACK.>"
              "_INBOX.>"
            ]
          }
          subscribe: {
            allow: [
              # The media plane's session lifecycle, taken as a core subscription rather than a
              # durable consumer on purpose.
              "media.evt.v1.>"
              # The SIGNALLING plane's dialog lifecycle, on the same core subscription and for the
              # identical reason: the engine wants a leg torn down NOW and must not pay for an ack
              # round trip on the call path. The SIP stream sits behind it so a `dialog.terminated`
              # the engine missed is still CDR evidence somebody can replay — that event carries the
              # only real Q.850 cause this platform ever sees, because it is the only place a SIP
              # response was actually read.
              "sip.evt.v1.>"
              # An arriving INVITE, from apps/sipd. SUBSCRIBE only, on the same asymmetry as the
              # REFER further down: the reply goes to the requester's `_INBOX.>`. Served on a queue
              # group, and the subject is FLAT rather than instance-addressed like the four
              # `rpc.sip.v1.` commands on the publish side — an arriving call has no owner yet, so
              # whichever engine answers BECOMES the owner, which is exactly what a queue group
              # picks for you.
              #
              # It decides ADMISSION only, never the outcome. A routing walk rings for thirty
              # seconds, so a request-reply that returned the call's result would need a
              # sixty-second deadline, and a request-reply with a sixty-second deadline is a
              # subscription wearing one's clothes on a transport with no redelivery.
              "rpc.sip.v1.invite"
              # The park handoffs it answers for the calls it holds. See the publish side above.
              "rpc.engine.v1.park-handoff.*"
              # Click-to-call, from apps/api. SUBSCRIBE only, on the same asymmetry as the REFER
              # below: the reply goes to the requester's `_INBOX.>`. Served on the queue group
              # `optimiq-engine-originate`, and the subject is FLAT rather than instance-addressed
              # like `park-handoff` above — an originate CREATES the call, so any instance may
              # answer and exactly one must, which is what a queue group produces.
              "rpc.engine.v1.originate"
              # The session protocol's verbs, from apps/api. SUBSCRIBE only, on the same asymmetry
              # as everything else here. Instance-addressed rather than queue-grouped like the
              # originate above it, and for the reason the park handoff states: a leg lives on ONE
              # instance's media channel, and a queue group would hand the command to whichever
              # engine answered first — which, on a fleet of eight, is the right one once.
              "rpc.engine.v1.session-verb.*"
              # In-conference moderation, from apps/api. SUBSCRIBE only, on the same asymmetry.
              # Instance-addressed for a reason worth stating in the conference's own terms: a room
              # is JOINTLY held — several instances have members in it — so there is no instance that
              # owns "the conference", but every MEMBER is on exactly one instance's media channel
              # and only that instance can mute it. The api addresses each contributor it read out of
              # the `conference-claims` value in turn.
              "rpc.engine.v1.conference-control.*"
              # A desk phone's REFER, relayed by apps/sipd. SUBSCRIBE only, and that asymmetry is
              # the shape of every responder here: the reply goes to the requester's `_INBOX.>`,
              # which the publish list already grants, so this subject never appears there. Served
              # on the queue group `optimiq-engine-sip-transfer`; a queue group needs no permission
              # of its own, the subject is the permission.
              "rpc.sip.v1.transfer"
              # The buckets it reads and watches.
              "$KV.routing-cache.>"
              "$KV.did-index.>"
              "$KV.queue-membership.>"
              "$KV.channels.>"
              "$KV.agent-state.>"
              "$KV.park-claims.>"
              "$KV.conference-claims.>"
              # The waiting line. Written by EVERY instance holding a caller for a queue, under
              # compare-and-set — it is jointly held rather than claimed, which is why it sits here
              # and not with the two claim buckets it is listed beside.
              "$KV.queue-waiting.>"
              # Read as well as written — see the publish side.
              "$KV.presence.>"
              # RPC replies, JetStream replies, KV watch deliveries.
              "_INBOX.>"
            ]
          }
        }
      }

      # ---------------------------------------------------------------------------------------
      # apps/mediad — the media plane. The narrowest identity on the broker, and the one that
      # matters most: this is the process with RTP sockets open to the internet.
      #
      # It answers eleven commands, emits four session-lifecycle events and owns one KV bucket. It
      # makes no requests at all, so it needs no publish grant on any `rpc.` subject.
      {
        user: $NATS_MEDIAD_USER
        password: $NATS_MEDIAD_PASS
        permissions: {
          publish: {
            allow: [
              # `session.ended`, `session.rtp-timeout`, `playback.finished` and
              # `recording.finished`. The event token is DOTTED, so the subject has six tokens and
              # `media.evt.v1.*.*.*` would match none of them — which is also why rung 1's third
              # event and rung 4's fourth needed no change here.
              "media.evt.v1.>"
              # Its bucket, and only its bucket.
              "$KV.media-sessions.>"
              # The exact JetStream surface, enumerated because it is small and static: bucket
              # open (INFO then CREATE-or-UPDATE), and the two read paths a KV `Get` may take.
              "$JS.API.INFO"
              "$JS.API.STREAM.INFO.KV_media-sessions"
              "$JS.API.STREAM.CREATE.KV_media-sessions"
              "$JS.API.STREAM.UPDATE.KV_media-sessions"
              "$JS.API.STREAM.MSG.GET.KV_media-sessions"
              "$JS.API.DIRECT.GET.KV_media-sessions.>"
              # `msg.Respond` publishes to the ENGINE's inbox, and JetStream publish acks come
              # back on this one.
              "_INBOX.>"
            ]
          }
          subscribe: {
            allow: [
              # The command surface, on queue group `mediad`. A queue group needs no permission
              # of its own — the subject is the permission.
              "rpc.media.v1.allocate-session"
              "rpc.media.v1.bridge-sessions"
              "rpc.media.v1.unbridge-sessions"
              "rpc.media.v1.release-session"
              "rpc.media.v1.start-playback"
              "rpc.media.v1.stop-playback"
              "rpc.media.v1.send-dtmf"
              "rpc.media.v1.start-recording"
              "rpc.media.v1.stop-recording"
              # The supervision pair, on the queue group `mediad` like the nine above it. Named here
              # for the same reason they are — this list is the exact command surface, and a command
              # added to the Go code without being added here fails at boot with an authorization
              # error naming the subject, which is the right way to find out.
              #
              # Worth stating plainly on the media plane's entry, since this is the process with RTP
              # sockets open to the internet: a tap is a one-way COPY of a session's audio into
              # another session, and it is the only command mediad answers that sends one party's
              # media somewhere neither party chose. Both halves are here because a tap that could be
              # started and not stopped is a leak with no off switch.
              "rpc.media.v1.tap-session"
              "rpc.media.v1.untap-session"
              # Rung 5's state pair, on the queue group `mediad` like the eleven above them. Named
              # here for the reason every line on this list is: a command added to the Go code
              # without being added here fails at boot with an authorization error naming the
              # subject, which is the right way to find out.
              "rpc.media.v1.mute-session"
              "rpc.media.v1.hold-session"
              "_INBOX.>"
            ]
          }
        }
      }

      # ---------------------------------------------------------------------------------------
      # apps/sipd — the SIP edge, and this entry is the one that changed shape most.
      #
      # It was described here, accurately, as "a pure producer plus two requesters: it subscribes to
      # no business subject at all", and later as a producer with two READ-ONLY consumers. **Neither
      # sentence is true any more.** With the INVITE path the edge became the platform's signalling
      # plane, and that is three new things at once:
      #
      #   * a PRODUCER of two more families — `sip.evt.v1` dialog lifecycle, and `trunk.evt.v1`
      #     reachability, which is the OPTIONS pinger's verdict. The second one is worth calling out
      #     because `internal/trunk/status.go` shipped defaulting to a logging publisher precisely
      #     BECAUSE this grant did not exist; the grant is on the publish list now, so that fallback
      #     stops being the only thing the FSM can do with a verdict.
      #   * a REQUESTER of a third RPC, `rpc.sip.v1.invite` — the admission question, and the first
      #     request this process makes that is on the call path rather than beside it.
      #   * a RESPONDER, which it has never been before. Five command subjects from apps/engine sit
      #     on the subscribe list below, and they are the first business subjects this identity has
      #     ever subscribed to. Everything it consumed until now was a KV watch or an `_INBOX.>`
      #     reply.
      #
      # It also holds five buckets rather than one: `registrations` and `sip-dialogs` written,
      # `presence`, `trunks` and `sip-acl` read. What it still cannot do is unchanged and is listed
      # in full at the top of this file — no CDR leg, no call event, no media command.
      {
        user: $NATS_SIPD_USER
        password: $NATS_SIPD_PASS
        permissions: {
          publish: {
            allow: [
              # `registered` / `unregistered` / `expired`, acked into the REGISTRATIONS stream.
              "sip.reg.v1.>"
              # SIP DIALOG lifecycle, acked into the SIP stream. A second root under `sip.` rather
              # than more event names under `sip.reg.v1`, because the two differ by two orders of
              # magnitude in volume and by their whole purpose — a phone re-registers every few
              # minutes, a busy tenant sets up thousands of calls an hour — and because a
              # `dialog.terminated` is CDR evidence while a `registered` is presence. Different
              # streams, different retention, and one subject root each.
              "sip.evt.v1.>"
              # Trunk REACHABILITY, from the registration FSM. The edge is the authoritative
              # producer and always was: the process that sends the REGISTER and reads the
              # carrier's answer is the only thing that knows whether a trunk is up. apps/api holds
              # no grant on this family's publish side — it writes the trunk's CONFIGURATION into
              # the `trunks` bucket, which is the other half and changes for entirely different
              # reasons.
              "trunk.evt.v1.>"
              # The two requests it makes. SIP password derivations from apps/api, active only when
              # SIPD_CREDENTIAL_SOURCE=nats; and a desk phone's REFER handed to apps/engine, which
              # is the edge's ONLY publish onto a subject the call path serves. Both are core
              # request-reply, so the reply arrives on `_INBOX.>` below and neither needs a
              # subscribe grant here.
              "rpc.sip.v1.credential"
              "rpc.sip.v1.transfer"
              # An arriving INVITE, handed to apps/engine for admission. The edge's THIRD request
              # and the one on the call path: it asks "will you take this call at all", and the
              # answer names the tenant, the call id and the engine instance that took it.
              #
              # Core request-reply like the two above, so the reply arrives on `_INBOX.>` and this
              # needs no subscribe grant. A request that goes unanswered is answered `503` by this
              # edge on its own authority and logged as DISTINCT from a refusal — a refusal is
              # always a reply and never a silence, and a silent engine would leave this process
              # holding an INVITE server transaction until the caller's Timer B, which is
              # thirty-two seconds of nothing on a handset.
              "rpc.sip.v1.invite"
              # Its bucket, and only its bucket. Note what is NOT here: `$KV.presence.>`. A KV write
              # is a publish onto that subject, so leaving it off is what makes sipd's access to the
              # presence bucket read-only — the SIP edge renders device state, it never asserts any.
              "$KV.registrations.>"
              "$JS.API.INFO"
              "$JS.API.STREAM.INFO.KV_registrations"
              "$JS.API.STREAM.CREATE.KV_registrations"
              "$JS.API.STREAM.UPDATE.KV_registrations"
              "$JS.API.STREAM.MSG.GET.KV_registrations"
              "$JS.API.DIRECT.GET.KV_registrations.>"
              # `bucket.Keys()` at boot (registration rehydration) opens an ANONYMOUS ordered
              # consumer. BOTH forms are needed and neither covers the other: a client that names
              # the consumer and its filter puts them after the stream as further tokens
              # (`…CREATE.KV_registrations.<name>.$KV.registrations.>`), while a client asking the
              # server to generate the name sends the BARE `…CREATE.KV_registrations` — and `>`
              # matches one-or-more tokens, never zero. Leaving the bare form out is a permissions
              # violation on the first rehydration, which is what the verification caught.
              "$JS.API.CONSUMER.CREATE.KV_registrations"
              "$JS.API.CONSUMER.CREATE.KV_registrations.>"
              "$JS.API.CONSUMER.DELETE.KV_registrations.>"
              "$JS.API.CONSUMER.INFO.KV_registrations.>"
              # The `presence` bucket, READ side only. Bucket open (INFO then CREATE-or-UPDATE) and
              # the two read paths a KV `Get` may take — the same five subjects as the registrations
              # bucket above, minus the `$KV.presence.>` write.
              #
              # CREATE/UPDATE are here and are not a write grant: they provision the BUCKET, not a
              # value in it. sipd opens it with CreateOrUpdateKeyValue for the reason kv.Open does —
              # an edge that refused to start because apps/engine had not booted yet would turn a
              # deploy ordering detail into a SIP outage — and the definition it applies comes from
              # packages/events-go, so it cannot create one the engine disagrees with.
              "$JS.API.STREAM.INFO.KV_presence"
              "$JS.API.STREAM.CREATE.KV_presence"
              "$JS.API.STREAM.UPDATE.KV_presence"
              "$JS.API.STREAM.MSG.GET.KV_presence"
              "$JS.API.DIRECT.GET.KV_presence.>"
              # `bucket.WatchAll()` — one watch per instance, feeding every BLF subscription — opens
              # an ANONYMOUS ordered consumer. BOTH forms are needed and neither covers the other:
              # a client that names the consumer and its filter puts them after the stream as further
              # tokens (`…CREATE.KV_presence.<name>.$KV.presence.>`), while a client asking the
              # server to generate the name sends the BARE `…CREATE.KV_presence` — and `>` matches
              # one-or-more tokens, never zero. This is the same trap the registrations rehydration
              # fell into; the shape is copied deliberately.
              "$JS.API.CONSUMER.CREATE.KV_presence"
              "$JS.API.CONSUMER.CREATE.KV_presence.>"
              "$JS.API.CONSUMER.DELETE.KV_presence.>"
              "$JS.API.CONSUMER.INFO.KV_presence.>"
              # The `sip-dialogs` bucket, WRITE side — the edge's second owned bucket, and the only
              # writer it will ever have.
              #
              # It is a CLAIM directory and not a call directory: which sipd holds which dialog,
              # under a heartbeated lease. Its job is REAPING, not failover, and the difference is
              # worth stating plainly here because the alternative is discovering it during an
              # incident. A dialog's transaction state, its timers and its socket are all local, so
              # a crashed sipd is N dropped calls and no grant could change that. What a survivor
              # CAN do is notice: a sipd that finds an expired claim it does not own publishes
              # `dialog.terminated{reason: "instance-lost"}` on the dead owner's behalf, and the
              # engine writes a CDR from a teardown it would otherwise never have received.
              # Without this bucket, a rescheduled pod's calls are channels the engine holds for
              # ever and rows nobody bills.
              "$KV.sip-dialogs.>"
              "$JS.API.STREAM.INFO.KV_sip-dialogs"
              "$JS.API.STREAM.CREATE.KV_sip-dialogs"
              "$JS.API.STREAM.UPDATE.KV_sip-dialogs"
              "$JS.API.STREAM.MSG.GET.KV_sip-dialogs"
              "$JS.API.DIRECT.GET.KV_sip-dialogs.>"
              # The reaping sweep walks keys, which opens an ANONYMOUS ordered consumer. BOTH forms
              # again, and neither covers the other — see the registrations note above for the trap.
              "$JS.API.CONSUMER.CREATE.KV_sip-dialogs"
              "$JS.API.CONSUMER.CREATE.KV_sip-dialogs.>"
              "$JS.API.CONSUMER.DELETE.KV_sip-dialogs.>"
              "$JS.API.CONSUMER.INFO.KV_sip-dialogs.>"
              # The `trunks` bucket, READ side only — bucket open and the two read paths, with no
              # `$KV.trunks.>` write. apps/api owns the write; see its publish list for why the
              # edge cannot simply read the database.
              #
              # CREATE/UPDATE here provision the BUCKET and are not a write grant on a value, the
              # same distinction the `presence` block above draws — and for the same reason: an
              # edge that refused to start because the control plane had not booted yet would turn
              # a deploy ordering detail into a SIP outage.
              "$JS.API.STREAM.INFO.KV_trunks"
              "$JS.API.STREAM.CREATE.KV_trunks"
              "$JS.API.STREAM.UPDATE.KV_trunks"
              "$JS.API.STREAM.MSG.GET.KV_trunks"
              "$JS.API.DIRECT.GET.KV_trunks.>"
              # `WatchAll()` at boot and for the life of the process, so a trunk edited in the admin
              # UI reaches the registration FSM without a restart — which is what replaces the
              # SIPD_TRUNK_ACL environment variable that could only be changed by redeploying.
              # Both consumer-create forms, same trap.
              "$JS.API.CONSUMER.CREATE.KV_trunks"
              "$JS.API.CONSUMER.CREATE.KV_trunks.>"
              "$JS.API.CONSUMER.DELETE.KV_trunks.>"
              "$JS.API.CONSUMER.INFO.KV_trunks.>"
              # The `sip-acl` bucket, READ side only, and this is the grant to look at hardest.
              #
              # These entries decide which strangers may send this platform an INVITE without
              # authenticating — the anti-toll-fraud boundary. The absence of `$KV.sip-acl.>` from
              # this list is therefore load-bearing in a way the other read-only grants are not: it
              # is what stops a compromised SIP edge, the process with a socket open to the
              # internet, from widening the ACL that admits it. Only apps/api may write here.
              #
              # The edge compiles the entries into an in-process longest-prefix match at boot and on
              # every watch update, rather than doing a KV get per INVITE. A get per INVITE would be
              # a broker round trip INSIDE a SIP transaction on the one code path whose rate an
              # attacker chooses — the cheapest denial of service available against this design,
              # bought for nothing.
              "$JS.API.STREAM.INFO.KV_sip-acl"
              "$JS.API.STREAM.CREATE.KV_sip-acl"
              "$JS.API.STREAM.UPDATE.KV_sip-acl"
              "$JS.API.STREAM.MSG.GET.KV_sip-acl"
              "$JS.API.DIRECT.GET.KV_sip-acl.>"
              "$JS.API.CONSUMER.CREATE.KV_sip-acl"
              "$JS.API.CONSUMER.CREATE.KV_sip-acl.>"
              "$JS.API.CONSUMER.DELETE.KV_sip-acl.>"
              "$JS.API.CONSUMER.INFO.KV_sip-acl.>"
              "_INBOX.>"
            ]
          }
          subscribe: {
            allow: [
              # ONE business subject, added with the presence spine: the mailbox counts that light a
              # voicemail lamp, taken as a CORE subscription rather than a durable consumer so a
              # restart does not replay a backlog of stale counts at every phone at once. `apps/api`
              # publishes it; sipd turns it into an RFC 3842 NOTIFY.
              #
              # `mwi.updated` is a DOTTED event name, so the subject has six tokens. The mailbox
              # wildcard cannot be a `>`: `voicemail.evt.v1.*.>` would also match `message.left`,
              # which is the engine's event about a recording and carries no counts at all.
              "voicemail.evt.v1.*.*.mwi.updated"
              # The dialog command surface it ANSWERS, from apps/engine. Subscribe only, on the
              # asymmetry every responder here has: the reply goes to the requester's `_INBOX.>`,
              # which the publish list already grants, so these subjects never appear there.
              #
              # The trailing token is THIS instance's id. A one-token wildcard rather than the
              # literal id because the id is assigned per pod and an enumeration would need a broker
              # restart every time one is rescheduled — the same shape, and the same reason, as the
              # engine's `rpc.engine.v1.park-handoff.*`. The grant is therefore wider than what any
              # single process subscribes to: each sipd subscribes its OWN token only, and the
              # narrowing is the application's, not the broker's. That is the same trade the engine
              # already makes on three subjects, and the thing it costs is worth naming — an
              # instance could subscribe a peer's command subject and steal its commands. It cannot
              # do anything useful with them: it does not hold the dialog, so it can only answer
              # `unknown_dialog`, which is what a stale addressing bug looks like anyway.
              "rpc.sip.v1.ring.*"
              "rpc.sip.v1.answer.*"
              "rpc.sip.v1.hangup.*"
              "rpc.sip.v1.reinvite.*"
              # Outbound, served on a queue group. FLAT rather than instance-addressed, and the one
              # command here that is: the four above act on a dialog that already exists on exactly
              # one process, while this one CREATES it and so has no owner to find. Any sipd may
              # place the call and exactly one must, which is what a queue group produces; the reply
              # carries the instance that took it. A queue group needs no permission of its own —
              # the subject is the permission.
              "rpc.sip.v1.originate"
              # The two read models it watches. Read-only: neither `$KV.trunks.>` nor
              # `$KV.sip-acl.>` appears on the publish list, and for `sip-acl` that absence is a
              # security boundary rather than tidiness. See the publish list.
              "$KV.trunks.>"
              "$KV.sip-acl.>"
              # Its own claim directory, read as well as written — a survivor sweeping a dead peer's
              # dialogs is the reader that matters, and it is a different process from the writer.
              "$KV.sip-dialogs.>"
              # Replies: the credential RPC's answer, the JetStream publish acks, the rehydration
              # consumer's deliveries and the presence watch's.
              "_INBOX.>"
            ]
          }
        }
      }
    ]
  }

  $SYS: {
    users: [
      { user: $NATS_SYS_USER, password: $NATS_SYS_PASS }
    ]
  }
}

# There is deliberately NO `no_auth_user`. That directive maps anonymous connections onto a named
# user and would reinstate exactly the exposure this file exists to close.
