name: deploy to github pages

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "apps/site/**"
      - "docs/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Populate out directory
        run: |
          # Create out directory
          mkdir -p out/site-assets

          # Copy site assets
          cp -a apps/site/site-assets/* out/site-assets
          cp apps/site/index.html out/index.html
          echo > out/.nojekyll
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
          publish_branch: gh-pages
          cname: optimiq-voice.com
