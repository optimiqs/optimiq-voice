# Repo-wide audit and fix pass — 2026-09-09

Baseline commit: 724d9f1 (the September 7–8 native-calling work, committed first). Every source file in
apps/* and packages/* was read by one of 13 parallel auditors, each owning a disjoint directory set;
13 fix agents then landed the findings area by area, followed by an integration wave for the items
that crossed area boundaries. Six commits, 6ccfef8..e5cc097, one per area.

## Findings

229 findings, all with file:line evidence, a failure scenario and a proposed fix. Severity mix:

| Severity              | Count | Fixed | Skipped or deferred |
| --------------------- | ----: | ----: | ------------------: |
| P0 bug / security     |    36 |    36 |                   0 |
| P1 perf / correctness |    90 |    88 |                   2 |
| P2 quality            |   103 |    92 |                  11 |

Six findings were judged wrong on inspection and left alone (reasoning in each area's report under
`scratchpad/audit/FIX-*.md`): the API export-worker `MAX_ATTEMPTS` off-by-one, the reseller
member-count loop, the voicemail consumer tenancy check, the better-auth API-key `organizationId`
omission, the follow-me positional error keys, and the root tsconfig strictness claim.

## The P0s worth knowing about

Security

- SSO: tenant IdPs were registered platform-wide with account linking on. An org admin controlling
  an IdP could assert another tenant's email and take over that account.
- `moh_class.streamUri` was rendered unvalidated into `musiconhold.conf`; a newline injected an
  `application=` line that Asterisk executes.
- mediad built the recording directory from unvalidated `orgId`/`callId`, so `../../..` escaped the
  recordings root.
- sipd accepted a replayed REGISTER `Authorization` with a different Contact within the nonce TTL,
  forking a victim's inbound calls to the attacker.
- `branding.logoObjectKey` was caller-supplied and then served unauthenticated from the shared media
  root; SVG logos were served inline on the API origin.
- api and engine held `$JS.API.>` in `config/nats.conf`, which made their enumerated NATS grants
  decorative (stream delete/purge, and reads of every KV bucket including registrations).
- Effect defect logging and `Effect.log` annotations bypassed redaction entirely.
- Unmatched WebSocket upgrades were never destroyed once an upgrade listener existed: an
  unauthenticated fd leak. The provisioning rate limiter ran after the secret check.

Correctness

- sipd's carrier profile was unreachable in its own default configuration (no listener means never
  matched), so every carrier INVITE got an unanswerable digest challenge.
- The engine's AOR fallback applied to off-net follow-me hops, external destinations and external
  queue agents, resolving them against the tenant registrations bucket instead of the trunk.
- One tenant's first `allow` ACL row flipped the shared Asterisk ACL section to allowlist mode with
  `deny 0.0.0.0/0`, locking every other tenant's phones out.
- The projection outbox was discharged even when every KV put failed (trunks, DID index, SIP ACL,
  queue membership), so a failed publish was never retried.
- `PUT …/reorder` violated the unique `(parent, ordinal)` index on 7 of 8 reorderable collections.
- `/healthz` did not see the sipd dialog feed; a dead feed meant no leg ever ended while the pod
  reported healthy. Live media events were dispatched unordered per leg.
- `planNodeReferences` omitted four edge kinds, so closure and cycle checks were blind to queue exit,
  call-flow day/night, stream fallback and dial-by-name branches.
- mediad never reaped held or muted sessions (a ringing leg after an engine crash leaked its port
  pair until restart) and grew the playback index on every hold.
- A data race in sipd's reaper heartbeat read live dialog fields off the owning goroutine.

## Performance highlights

- mediad transcode hot path: 3 allocations / 1120 B per packet down to 1 / 160 B (measured).
  Mixer socket writes moved outside the conference mutex; allocator binds outside its mutex.
- Engine queue polling collapsed from `callers × agents` KV gets per second to one fan-out per roster
  per poll; `rankOf` is a counting pass instead of a full re-sort per caller; routing-artifact and
  roster caches are TTL + LRU bounded.
- sipd registrar sweep no longer does a KV round trip per binding every 5 s; credential cache has
  O(1) eviction and single-flight; ACL replay compiles once instead of once per record.
- API: 21 serial per-delete existence scans became one `union all`; webhook selectors compile once
  per subscription; per-key KV gets are batched; WS gateways revalidate every 5 minutes instead of
  every 25 seconds.
- Web live store returns the same state when a KV put is unchanged (was ~5 full-table re-renders/s
  for 300 phones on a 60 s Expires); extra topic leases replay a cached snapshot instead of
  re-subscribing (was O(N²) on mount).

## Verification on the committed tree

| Check                           | Result                                                                                                                                                                       |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| turbo build + typecheck + test  | 48/48 tasks                                                                                                                                                                  |
| api (mocha)                     | 1213 passing (baseline 1166)                                                                                                                                                 |
| engine (bun)                    | 1575 pass (baseline 1545)                                                                                                                                                    |
| web (bun)                       | 764 pass (baseline 741)                                                                                                                                                      |
| packages (bun)                  | routing 859 · events 387 · auth 230 · telephony 242 · pbx-db 108 · db 96 · media-ari 87 · telnyx 81 · cdr-db 73 · config 36 · logging 25 · effect-runtime 12 · identifiers 6 |
| pbx-db / cdr-db RLS integration | 120 / 100 pass against a real Postgres                                                                                                                                       |
| Go `go vet` + `go test -race`   | sipd 18 pkgs ok · mediad 6 pkgs ok · events-go ok · runtime-go ok                                                                                                            |
| sipd gated integration suite    | 14/14 against a real nats-server, 3 of them under `config/nats.conf` with the platform identities                                                                            |
| mediad browser WebRTC suite     | pass                                                                                                                                                                         |
| events codegen drift            | none; web permissions SDK up to date                                                                                                                                         |
| oxlint / oxfmt (`format:check`) | 0 errors, all files formatted                                                                                                                                                |
| compose configs                 | 4/4 validate; `nats-server -t` accepts the new conf                                                                                                                          |

## Deferred with reasons (needs a decision or a larger change)

- Five `ON DELETE SET NULL` foreign keys were not made tenant-composite: a composite `SET NULL` would
  null `organization_id`, and PG15's `SET NULL (column)` is not expressible in drizzle-kit.
- `organization_sso_provider.provider_id` stays globally unique. better-auth uses it in the callback
  URL, so a per-org composite would need a derived global id; duplicates return 409 for now.
- Per-tenant named Asterisk ACLs were not built (they need one endpoint per tenant, which nothing
  generates); the multi-tenant section degrades to blocklist rendering with a warning instead.
- The sipd reaper still lists the whole `sip-dialogs` bucket per instance; a watch-backed index or
  leader lease is a design change. The reap now runs on a longer jittered interval.
- Engine ownership maintenance still does `keys()` + per-key `get`; the pass now skips foreign
  leases, which removes most round trips. A watch-backed cache is the full fix.
- The routing `explain` flag for discarded diagnostics needs both `packages/routing` and
  `apps/api/src/pbx/routing/routing.service.ts` landed together.
- `mediad BuildAnswer` Opus/PT-0 refusal needs a signature change across callers; the audit
  confirmed it is latent.
- The child-DELETE guard split (pin-sets/phrases on `.delete` vs `.write`) was left as is; the
  proposed fix lowered a permission with no bug behind it.
- Web CSP is report-only: the softphone WSS host and the signed recording URLs are per-deployment
  values that cannot be verified from the codebase. HSTS is enforced.

## Judgement calls to review

- Conference join destroys the bridge only when this leg created it; destroying unconditionally
  would end a meeting other members are already in.
- `releaseAll` gives the hangup cause only to the agent it belongs to rather than widening
  `QueueDialOutcome` to a per-attempt list.
- The voicemail "Messages…" row action gates on `voicemail.read.own` (it opens a list); play and
  edit gate on `listen.own` / `write.own` inside the dialog.
- `ENGINE_INSTANCE_ID` set to the literal `engine` is refused in production. Containers get
  `HOSTNAME`; a bare-metal production run must set one of the two.
- `dockerize` was removed from the API image in favour of `depends_on: service_healthy`.
- The branding `logoObjectKey` DTO field accepts only `null` (to clear the logo) rather than being
  removed, because there is no `DELETE /branding/logo` route.

## Operational notes

- `config/nats.conf` is validated syntactically and by sipd's integration suite under the platform
  identities. The api and engine identities have not been exercised against a live broker; run
  `.scripts/verify-platform-stack.mjs` on a Docker host before deploying the new grants.
- New migrations: `db` (rate_limit table), `pbx-db` (tenant-composite FKs), `cdr-db`
  (export object_key check). All applied to disposable databases during verification.
- The full audit reports live outside the repo in the session scratchpad; each fix commit message
  carries the substance.

## Networking performance pass (same day, commits 781973a..1101eb8)

Every network path got a real load harness against real sockets and a real nats-server, was
profiled before any change, fixed where the profile pointed, and re-measured. The harnesses stay in
the repo so the numbers can be reproduced:

| Path                                                | Harness                                                                       | How to run                                                                                       |
| --------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| RTP relay, mixer, transcode, RTCP                   | `apps/mediad/internal/rtp/bench_test.go`, `loadtest_test.go`                  | `go test -bench . -benchmem ./internal/rtp/`; `go test -tags loadtest -run Load ./internal/rtp/` |
| WebRTC + control RPC                                | `apps/mediad/internal/webrtc/bench_test.go`, `internal/control/bench_test.go` | `go test -bench . ./internal/...`; `RUN_BROWSER_WEBRTC=1` for the Chromium suite                 |
| SIP edge (REGISTER, INVITE, carrier auth, presence) | `apps/sipd/load_*_test.go`                                                    | `RUN_SIPD_LOAD=1 NATS_SERVER_BIN=<nats-server> go test -tags load -run Load ./...`               |
| Engine NATS layer                                   | `apps/engine/bench/*.ts`                                                      | `NATS_SERVER_BIN=<nats-server> bun run bench/<script>.ts`                                        |
| API live gateway                                    | `apps/api/scripts/bench-live-fanout.ts`, `bench-live-snapshot.ts`             | `pnpm --filter @optimiq-voice/api exec tsx scripts/bench-live-fanout.ts`                         |

Headline results (allocation and delivery numbers are exact; timings on a shared box carry noise,
which each report states):

- mediad relay packet path: 176 B / 1 alloc to 0 B / 0 allocs per packet; conference tick at 8
  members 3 940 B / 31 allocs to 33 B / 8 allocs; transcode and RTCP reports allocation-free.
  500 bridged pairs at 50 pps for 12 s on loopback with 0% loss.
- mediad control: a cross-node forward ran on the NATS dispatcher goroutine, so a wedged owner
  stalled unrelated local holds from 1.957 s to 474 µs after moving forwards off the dispatcher.
  WebRTC inbound 2 458 to 772 B per packet.
- sipd: REGISTER broker round trips 3 to 2 (KV hint plus async publish), +20% registrations/s,
  −28% p99; dialogs/s +43%. Presence fan-out was shedding two thirds of every burst because a
  concurrency bound sized for changes was applied per NOTIFY; now 100% delivered at 11× the
  NOTIFY rate.
- engine: the channel-snapshot KV scan read at most one channel (awaiting a get inside the keys()
  iteration ends the ordered consumer); now 300/300. Publish path parses each envelope once,
  −30% CPU floor. 10k events/s at p99 2 ms; 14.8k RPC/s at p99 0.43 ms.
- api: the same KV scan bug made wallboards open on partial registration tables; fixed there and
  in the SIP ACL and DID index publishers. Live fan-out encodes once per topic (CPU per event
  1 121 to 308 µs at 500 tabs), slow clients are closed at 4 MiB buffered instead of growing
  server memory unbounded, role-permission resolution memoised (1.8 µs to 3 ns), and the SIP
  credential responder answers up to 32 requests concurrently instead of one at a time.

Also in this pass, per the user's instructions: the Modern Go Guidelines CLI was applied across
sipd, mediad, events-go and runtime-go, and Go comment lines were cut by roughly a third (sipd
7 116 to about 4 800, mediad 5 793 to about 3 700, events-go 832 to 621 hand-written) with 28
factually wrong comments corrected and no behaviour changes in the comment-only edits.

Recorded as recommendations, not done: folding the per-session RTCP goroutine into the read loop
(needs a WebRTC read deadline), a single-port ICE UDP mux (changes port-range config, compose and
docs), SO_REUSEPORT for the single sipd UDP socket, caching the carrier credential at the SIP edge
(a secret-boundary decision), and a Postgres-backed load test for the better-auth pool ceiling.

## End-to-end campaign on the real local stack (2026-09-09, later the same day)

The whole platform was brought up natively on non-default ports (`.scripts/local-stack/`, runbook in its README)
against Postgres in Docker on 5533 and a real nats-server running the production `config/nats.conf` on 4322. Every
capability was then exercised through the real product paths: Playwright browser softphones, Go SIP user agents
(`apps/sipd/internal/testutil/sipua`), a fake carrier on the external profile, the admin UI, and the public API.
Eighty-nine per-area reports with file:line evidence are in `plans/audit-2026-09-09/reports/`; the capability
checklist is `CAPABILITY-MATRIX.md` there (144 rows).

### What the live runs found and fixed

Correctness (each proved broken live, fixed with tests, re-proved live):

- Engine routing watch hung silently for hours: the engine and API NATS users lacked the `$JS.FC.>` flow-control
  reply grant, so ordered consumers stopped pushing without error. Grant added, plus a revision-based stale guard.
- KV listings that awaited a `get` inside `keys()` read one row instead of all, in the engine's channel scan, the
  API live snapshot and two publishers.
- sipd served the plaintext WS listener over TLS whenever WSS was enabled; sipd's ACL/trunk/presence watches died
  after a broker reconnect and never recovered; a revoked ACL rule stayed in force forever; sipd picked the SIP
  profile from the sender-supplied Request-URI, letting a trunk ACL entry turn another network's phones into
  digest-free carrier peers.
- A new org was handed another tenant's SIP realm; `ENGINE_SIP_REALM` removed, refusal by name when no domain is set.
- Tenant-uploaded prompts had never been playable on mediad (row id vs object key); default MOH and system prompts
  did not exist, so queues and IVRs played silence; the browser softphone sent DTMF as SIP INFO, so IVR digits never
  reached the far end; an unplayable IVR greeting hung up on the caller.
- Caller cancel never reached the ringing callee under the split plane; refused originates and every unanswered leg
  were filed as `answered` in the CDR; busy was relayed as 480; pickup bridged the caller unanswered; the softphone
  ignored auto-answer so paging never connected; park `*5` parked the presser and recalled the wrong party.
- A mediad crash stranded every call (the engine's hangup path needed mediad); a sipd crash left zombie calls; a dead
  engine's calls survived but were never billed. Fixed with sipd and engine instance leases and lease-fenced
  adoption: calls end in 1.3 s / 12.6 s after a plane dies, and a killed engine's calls are adopted in 12 s and billed.
- The `tsx` dev runner emitted no decorator metadata, so bare-class Nest injections were `undefined` in development;
  both apps now run on `@swc-node/register` and CI carries a DI-metadata guard.
- Every REGISTER burst was answered 403 when the credential RPC missed its deadline; the responder answered one
  lookup at a time behind three database transactions.

Performance, measured before and after on the same harness:

| Path                                       | Before                                     | After                    |
| ------------------------------------------ | ------------------------------------------ | ------------------------ |
| Engine INVITE admission (100 concurrent)   | setup-to-ring p99 819 ms, 2 lost walks     | p99 170 ms, 0 lost       |
| mediad `create-offer` p99 (200 concurrent) | 58 ms                                      | 13 ms                    |
| sipd `resolve-target` p99 (200 concurrent) | 103 ms                                     | 3.8 ms                   |
| API credential lookups                     | 474/s, p99 841 ms                          | ~40,000/s, p99 9 ms      |
| sipd REGISTER                              | 3 broker round trips                       | 2; +20% regs/s, −28% p99 |
| sipd presence fan-out                      | 33% NOTIFYs delivered                      | 100%, 11× rate           |
| API live WebSocket fan-out                 | 1,121 µs CPU/event, unbounded slow clients | 308 µs, 4 MiB cap        |

RTP loss across every load run: 0 of 12.6 million packets. Goroutines and heap return to baseline after each run.

### Capabilities added to close the matrix

Backups and restore (rehearsed: 86 tables row-for-row), Prometheus `/metrics` on all four services, CDR retention
job, credential-spray lockout, SDES-SRTP for SIP phones over TLS, STIR/SHAKEN attestation captured into the CDR,
CLIR/PAI with `*67`/`*82` and a per-extension setting, early media relay, recording pause/resume (PCI) with an API
and softphone surface, encrypted SSO client secrets, per-device dispatchable location (Ray Baum), one E.164
normaliser, agent and call-volume reporting, porting and CNAM via Telnyx, IVR direct extension dial with
inter-digit timeout, `*65`/`*64` toggles, default feature codes seeded on org creation, shared lines (seize, hold,
retrieve, recall), queue callback, hot desking, voicemail forward/copy, attended transfer, `cdr.read.own`, branding
logo upload, softphone transfer/DND/park/recents/go-offline controls, and a live wallboard that actually connects.

### Verification on the final build

| Check                                                               | Result                                                                                                                              |
| ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| turbo build + typecheck + test (final tree)                         | 48/48 tasks                                                                                                                         |
| engine (bun)                                                        | 1,897 pass, 0 fail                                                                                                                  |
| api (mocha)                                                         | 1,461 passing, 0 failing                                                                                                            |
| web (bun)                                                           | 889 pass, 0 fail                                                                                                                    |
| routing / events / telephony / auth / db / pbx-db / cdr-db / telnyx | 922 / 418 / 259 / 233 / 111 / 112 / 76 / 95, all 0 fail                                                                             |
| Go (sipd 21 pkgs, mediad 8 pkgs, events-go, runtime-go)             | gofmt clean, vet clean on every tag set, `go test -race` ok                                                                         |
| DI metadata guards (api, engine)                                    | clean under the swc-node runner                                                                                                     |
| events codegen drift, web permissions codegen                       | none                                                                                                                                |
| oxlint / format:check / actionlint                                  | clean                                                                                                                               |
| Live stack on the final build                                       | all `/healthz` and `/metrics` 200, four authenticated NATS users, zero permission violations since the last reload                  |
| Load, 100 concurrent calls (final build)                            | setup-to-ring p50 113 ms, p99 170 ms; 0 lost of 195,812 RTP packets; goroutines back to baseline                                    |
| Load, 50 concurrent after the last restarts                         | p50 138 ms, p99 148 ms; 0 lost of 99,900                                                                                            |
| Backup → restore drill                                              | 86 tables row-for-row, runtime roles re-provisioned, 2.8 s / 2.3 s                                                                  |
| Resilience (final build)                                            | mediad kill → calls end in 1.3 s; sipd kill → 12.6 s; engine kill → adopted in 12 s and billed; NATS and Postgres restarts survived |

Capability matrix at the time of the audit: 144 rows, 57 proven, 34 unproven, 33 partial, 19 missing. Every P0 and
every P1 gap on that list has been implemented since; the live re-check rounds (`E2E-final.md` through
`E2E-final4.md` in the reports directory) record which rows were then proven on the running stack and which still
carried a defect.

### Still open

Closed in the final rounds (`E2E-final4.md` → `E2E-final6.md`), each proved on the running stack after a restart:
blind transfer initiated by the party who answered (audio follows, three legs under one call id); shared-line
hold → retrieve → hold → recall with the lamp naming the retrieving appearance; queue callback to an internal
caller dialled, connected and listed by `GET /queues/:id/callbacks`; early media for both SIP-UA and browser
callers (about 1,000 pre-answer RTP packets over a 20 s announcement, two-way after the 200, billing from the 200);
the softphone's recording indicator and pause/resume control for `calls.control` holders. The callback's
`related_call_id` was the last miss: the engine stamped it but did not read it back (one missing entry in the
arrival-variable list), fixed with a regression test; the live confirmation is `E2E-final6.md`.

Known, deliberately not done: shared-line barge-in (a multi-party bridge), queue-callback trunk failover, conference
entry/exit tones (needs a room-level playback in the mixer; plan in `FIX-go-concurrency.md`), mobile clients, and
Telnyx-side items (STIR/SHAKEN signing, number porting execution) that require the carrier account. The local stack
carries ten fixture trunks with blank auth users that sipd refuses at load; the API now rejects creating such trunks.
`smoke-call.mjs` needs a Docker host to run the full-stack harness; the e2e Go suites need `NATS_SERVER_BIN`.

## Response to the independent SIP/media review (2026-09-09, evening)

An external review of sipd and mediad (`output/sipd-mediad-review-2026-09-09/review.md`, with reproduction probes)
reported 24 findings R01–R24 and twelve failing behavioural probes plus a race. Baseline on HEAD confirmed all twelve
probe failures and the race exactly as reported. Five agents fixed the findings by file ownership; every probe now
passes against the fixed tree (two probes needed their hand-built struct literals adapted to new field shapes, no
assertion changed) and each assertion was promoted into a regression test beside the implementation. Per-finding
detail is in `plans/audit-2026-09-09/reports/REVIEWFIX-{A,B,C,D,E,A2}.md`.

| Finding                                                      | Outcome                                                                                                                                                                       |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R01 recording pause replayed paused audio                    | Fixed: capture-time epoch on every frame, refused on enqueue, backlog discarded on the paused tick                                                                            |
| R02 wrong remote tag matched an established dialog           | Fixed: early index holds only dialogs whose remote tag is unresolved; full RFC 3261 §12 triple otherwise                                                                      |
| R03 retried secure allocations advertised a different key    | Fixed: session-owned, versioned negotiation; identical retry replays the committed SDP and crypto context                                                                     |
| R04 claim deleted before durable ack                         | Fixed: finalisation outbox publishes, waits for the ack, then deletes; reaper waits too                                                                                       |
| R05 dialog store read raced the owner                        | Fixed: owner-published immutable membership snapshots; race probe clean                                                                                                       |
| R06 runners bounded execution, not waiting work              | Fixed: shared bounded executor (total/per-key pending limits, deadlines, drain, multi-key reservation) in both services; NATS answers `capacity`/`shutting_down`              |
| R07 arrival metadata per peer, not per message               | Fixed: provenance travels on the datagram as a stripped-then-stamped header; stream transports keep the per-connection table by design                                        |
| R08 same-codec relay kept the source PT                      | Fixed: audio remapped to the destination's negotiated PT on the zero-decode path                                                                                              |
| R09 one packet assumed to be 20 ms                           | Fixed: decode actual sample counts, repacketise by media time in transcoder, recorder and conference members                                                                  |
| R10 inactive session could not resume                        | Fixed: mode derived from the two direction gates; one mechanism                                                                                                               |
| R11 overlapping resource sets ran concurrently               | Fixed: reference commands resolve to their session; executor reserves the full resource set                                                                                   |
| R12 bridge id reuse left the old pair connected              | Fixed: old pair detached before replacement                                                                                                                                   |
| R13 failed conference join mutated the old conversation      | Fixed: prepare then commit under lock with pointer/closed revalidation                                                                                                        |
| R14 empty conference loops leaked on several departure paths | Fixed: one departure operation returning cleanup work                                                                                                                         |
| R15 local session-timer refresher not implemented            | Refused with evidence: the refresh offer cannot be built at the edge; config and negotiation no longer volunteer the role; the unreachable effect errors                      |
| R16 NAT keepalive/clamp helpers without consumers            | Registration clamp wired into granted expiries; keepalive policy removed from config and docs                                                                                 |
| R17 four divergent digest flows                              | Fixed: one DigestGate for REGISTER/INVITE/SUBSCRIBE/REFER; SUBSCRIBE and out-of-dialog REFER had no lockout at all; source identity without port churn; realm-scoped accounts |
| R18 lifecycle events unordered, failures unrecovered         | Fixed: per-session ordered acknowledged publication with stable ids, critical vs telemetry, drain coordinated with admission                                                  |
| R19 topology scans under the manager lock                    | Fixed: reverse indexes; bridge lookup 48.6 µs at 10k bridges → 3.7 ns                                                                                                         |
| R20 quadratic mix-minus in the common case                   | Fixed: restricted-speaker sub-list, skipped when empty; 128 members 730 → 660 µs per tick                                                                                     |
| R21 jitter target ignored live playout; reorder stat wrong   | Fixed: bounded stretch/shrink debt applied to live playout; reorder measured against arrival order                                                                            |
| R22 serial claim heartbeats could starve the tail            | Fixed: bounded worker pool, separate budgets, fairness cursor; live owner's expired claim no longer reaped                                                                    |
| R23 orphan termination retries had no stable id              | Fixed: UUID v5 derived from leg incarnation + termination kind                                                                                                                |
| R24 comments promised guarantees the code lacked             | Fixed in every touched file; sipd README rewritten to the service as it is                                                                                                    |

Also found on the way: the review's flaky real-socket profile test now binds its own sockets and asserts serve
errors, and every sipd listener binds its own socket synchronously before serving, so sipgo 1.4.3's
`ListenAndServe` startup race is unreachable (`REVIEWFIX-E2.md`). The one test failure the full mediad run
surfaced after the fixes was a race in the bridge test rig, not in production code: the rig could hand the far end
the latch frame as its first packet; it now has a barrier and an assertion that no pre-bridge packet is relayed
(`REVIEWFIX-A3.md`). Shared primitives added for the fixes: `runtime-go/keyed` (bounded multi-key executor) and
`runtime-go/ackpub` (ordered acknowledged publication), adopted by both services.

Live round on the restarted sipd and mediad (`E2E-review-live.md`), 11 of 11: recording pause now yields exactly
zero energy for the whole paused interval with both ends playing tones; SDES over TLS with duplicated
allocate/create-offer commands returns byte-identical answers and audio still decrypts; a forged BYE with a wrong
remote tag is answered 481 and the call stays up; a 400-command burst is answered in full with `capacity` refusals
and goroutines back to baseline; registration expiry is clamped; wrong-password SUBSCRIBE and REFER count toward one
lockout; a teardown during a 5 s broker stall produces exactly one `dialog.terminated` and deletes the claim only
after the ack; a 30 ms and a 20 ms member mix in one room at the right pitch with a recording of ratio 1.000 to wall
time. Not driven live: create-offer's SDES branch, which this stack does not enable (`MEDIAD_SRTP_POLICY=require`).
